<schema lang="ts-zod">
import { z } from 'zod';

const UserSchema = z.object({
  name: z.string(),
  email: z.email(),
});
</schema>

<template>
<div class="user-profile">
  <h1>{{ user.name }}</h1>
  <p>{{ user.email }}</p>
</div>
</template>

<logic>
# This component displays a user profile
# Expects: user object with name and email
</logic>
